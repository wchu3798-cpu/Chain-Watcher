import React, { useState } from 'react';
import { Server, Database, Zap, MessageCircle, Bell, Shield, Cloud, Eye, TrendingUp } from 'lucide-react';

const IdealArchitecture = () => {
  const [selectedComponent, setSelectedComponent] = useState(null);

  const architecture = {
    chatgpt: {
      name: "ChatGPT App",
      role: "User Interface & Funnel",
      icon: MessageCircle,
      color: "green",
      responsibilities: [
        "Natural language queries",
        "Format & present data",
        "User acquisition (100M+ users)",
        "Payment collection (ChatGPT Plus)",
        "Authentication",
        "Conversation history"
      ],
      doesNOT: [
        "Real-time monitoring",
        "Store blockchain data",
        "Run 24/7 processes",
        "Connect to Ethereum directly"
      ],
      example: `User: "What happened in the last hour?"
GPT → Calls API → Gets: 3 alerts
GPT: "⚠️ 3 threats detected:
1. Burner wallet on UniV2 (Score: 67)
2. Failed tx from new address (Score: 55)
3. Unusual gas spike (Score: 51)
Would you like details on any?"`
    },
    api: {
      name: "Serverless API",
      role: "Query Interface",
      icon: Cloud,
      color: "blue",
      responsibilities: [
        "Serve alert data via REST",
        "Handle ChatGPT requests",
        "Rate limiting",
        "Analytics/logging",
        "Webhook endpoints",
        "User management"
      ],
      doesNOT: [
        "Monitor blockchain",
        "Store all transaction data",
        "Long-running processes"
      ],
      example: `GET /api/alerts?since=1h
→ Returns: [{...}, {...}, {...}]

POST /api/analyze
→ Returns: {risk_score: 67, ...}

GET /api/contracts/health
→ Returns: {status: "normal", ...}`
    },
    monitoring: {
      name: "Background Monitor",
      role: "Real-time Detection Engine",
      icon: Eye,
      color: "purple",
      responsibilities: [
        "24/7 blockchain monitoring",
        "Smart detection analysis",
        "Write alerts to database",
        "Learn baselines",
        "Send Telegram alerts",
        "Update reputation scores"
      ],
      doesNOT: [
        "Handle user queries",
        "Serve web requests",
        "Need to scale horizontally"
      ],
      example: `MONITORING LOOP:
1. Fetch new block
2. Filter for target contracts
3. Analyze with smart detection
4. Write alerts to DB
5. Send critical alerts to Telegram
6. Sleep 12 seconds
7. Repeat`
    },
    database: {
      name: "Database",
      role: "Single Source of Truth",
      icon: Database,
      color: "orange",
      responsibilities: [
        "Store alerts",
        "Store reputation data",
        "Store baselines",
        "Store user subscriptions",
        "Queryable by API"
      ],
      doesNOT: [
        "Process transactions",
        "Directly accessible by ChatGPT"
      ],
      example: `Tables:
- alerts (detected threats)
- reputation (address history)
- baselines (learned patterns)
- subscriptions (paid users)
- contracts (monitored targets)`
    }
  };

  const benefits = [
    {
      title: "Separation of Concerns",
      description: "Each component does ONE thing well",
      icon: Shield,
      examples: [
        "ChatGPT = UI only (no blockchain logic)",
        "Monitor = Detection only (no web requests)",
        "API = Query only (stateless)",
        "Database = Storage only"
      ]
    },
    {
      title: "Independent Scaling",
      description: "Scale each piece independently",
      icon: TrendingUp,
      examples: [
        "10,000 ChatGPT users? → API scales automatically (serverless)",
        "Monitor 100 contracts? → Just run more monitor instances",
        "Database full? → Upgrade PostgreSQL tier",
        "Each scales without affecting others"
      ]
    },
    {
      title: "Reliability",
      description: "Failures are isolated",
      icon: Server,
      examples: [
        "API down? → Monitor keeps running, data keeps collecting",
        "Monitor crashes? → ChatGPT still serves existing data",
        "ChatGPT has issues? → Your monitoring is unaffected",
        "Database hiccup? → Monitor buffers, API retries"
      ]
    },
    {
      title: "Cost Optimization",
      description: "Pay only for what you use",
      icon: Zap,
      examples: [
        "API: $0 when idle, pennies per request",
        "Monitor: $5-20/month (single small VPS)",
        "Database: $10-50/month (managed PostgreSQL)",
        "ChatGPT: Free marketing + distribution"
      ]
    }
  ];

  return (
    <div className="w-full max-w-7xl mx-auto p-6 bg-gradient-to-br from-slate-50 to-blue-50">
      <div className="text-center mb-8">
        <h1 className="text-4xl font-bold text-slate-800 mb-2">
          ✨ The Ideal Architecture
        </h1>
        <p className="text-xl text-slate-600">
          ChatGPT as Funnel → Serverless API → Background Monitor
        </p>
      </div>

      {/* Architecture Diagram */}
      <div className="bg-white rounded-xl shadow-2xl p-8 mb-8">
        <h2 className="text-2xl font-bold text-slate-800 mb-6">
          How It All Connects
        </h2>
        
        <div className="space-y-8">
          {/* Top Row: User → ChatGPT */}
          <div className="flex items-center justify-center gap-4">
            <div className="text-center">
              <div className="w-20 h-20 rounded-full bg-slate-100 flex items-center justify-center text-4xl mb-2">
                👤
              </div>
              <div className="text-sm font-semibold">User</div>
            </div>
            <div className="text-3xl text-slate-400">→</div>
            <div 
              className="text-center cursor-pointer transform hover:scale-105 transition-all"
              onClick={() => setSelectedComponent('chatgpt')}
            >
              <div className={`w-32 h-32 rounded-xl bg-green-100 border-4 ${
                selectedComponent === 'chatgpt' ? 'border-green-500 shadow-xl' : 'border-green-300'
              } flex items-center justify-center mb-2`}>
                <MessageCircle className="w-16 h-16 text-green-600" />
              </div>
              <div className="font-bold text-green-700">ChatGPT App</div>
              <div className="text-xs text-green-600">User Interface</div>
            </div>
          </div>

          {/* Arrow Down */}
          <div className="flex justify-center">
            <div className="text-3xl text-slate-400">↓</div>
          </div>

          {/* Middle Row: API */}
          <div className="flex justify-center">
            <div 
              className="text-center cursor-pointer transform hover:scale-105 transition-all"
              onClick={() => setSelectedComponent('api')}
            >
              <div className={`w-32 h-32 rounded-xl bg-blue-100 border-4 ${
                selectedComponent === 'api' ? 'border-blue-500 shadow-xl' : 'border-blue-300'
              } flex items-center justify-center mb-2`}>
                <Cloud className="w-16 h-16 text-blue-600" />
              </div>
              <div className="font-bold text-blue-700">Serverless API</div>
              <div className="text-xs text-blue-600">Query Interface</div>
            </div>
          </div>

          {/* Arrow Down */}
          <div className="flex justify-center">
            <div className="text-3xl text-slate-400">↓</div>
          </div>

          {/* Bottom Row: Database + Monitor */}
          <div className="flex items-center justify-center gap-8">
            <div 
              className="text-center cursor-pointer transform hover:scale-105 transition-all"
              onClick={() => setSelectedComponent('database')}
            >
              <div className={`w-32 h-32 rounded-xl bg-orange-100 border-4 ${
                selectedComponent === 'database' ? 'border-orange-500 shadow-xl' : 'border-orange-300'
              } flex items-center justify-center mb-2`}>
                <Database className="w-16 h-16 text-orange-600" />
              </div>
              <div className="font-bold text-orange-700">Database</div>
              <div className="text-xs text-orange-600">PostgreSQL</div>
            </div>

            <div className="text-3xl text-slate-400">←→</div>

            <div 
              className="text-center cursor-pointer transform hover:scale-105 transition-all"
              onClick={() => setSelectedComponent('monitoring')}
            >
              <div className={`w-32 h-32 rounded-xl bg-purple-100 border-4 ${
                selectedComponent === 'monitoring' ? 'border-purple-500 shadow-xl' : 'border-purple-300'
              } flex items-center justify-center mb-2`}>
                <Eye className="w-16 h-16 text-purple-600" />
              </div>
              <div className="font-bold text-purple-700">Monitor</div>
              <div className="text-xs text-purple-600">24/7 Detection</div>
            </div>
          </div>

          {/* Bottom: Ethereum */}
          <div className="flex justify-center">
            <div className="text-3xl text-slate-400">↑</div>
          </div>

          <div className="flex justify-center">
            <div className="text-center">
              <div className="w-32 h-32 rounded-xl bg-slate-100 border-4 border-slate-300 flex items-center justify-center mb-2">
                <div className="text-5xl">⛓️</div>
              </div>
              <div className="font-bold text-slate-700">Ethereum</div>
              <div className="text-xs text-slate-600">Blockchain</div>
            </div>
          </div>
        </div>

        <div className="mt-6 text-center text-sm text-slate-600">
          Click any component to see details →
        </div>
      </div>

      {/* Component Details */}
      {selectedComponent && (
        <div className="bg-white rounded-xl shadow-xl p-8 mb-8">
          <div className="flex items-center gap-4 mb-6">
            {React.createElement(architecture[selectedComponent].icon, {
              className: `w-12 h-12 text-${architecture[selectedComponent].color}-600`
            })}
            <div>
              <h3 className="text-2xl font-bold text-slate-800">
                {architecture[selectedComponent].name}
              </h3>
              <p className="text-slate-600">{architecture[selectedComponent].role}</p>
            </div>
          </div>

          <div className="grid md:grid-cols-2 gap-6 mb-6">
            <div>
              <h4 className="font-bold text-green-700 mb-3">✅ Responsibilities</h4>
              <ul className="space-y-2">
                {architecture[selectedComponent].responsibilities.map((item, i) => (
                  <li key={i} className="flex items-start gap-2">
                    <span className="text-green-500 mt-1">•</span>
                    <span className="text-slate-700">{item}</span>
                  </li>
                ))}
              </ul>
            </div>

            <div>
              <h4 className="font-bold text-red-700 mb-3">❌ Does NOT Handle</h4>
              <ul className="space-y-2">
                {architecture[selectedComponent].doesNOT.map((item, i) => (
                  <li key={i} className="flex items-start gap-2">
                    <span className="text-red-500 mt-1">•</span>
                    <span className="text-slate-700">{item}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>

          <div className="bg-slate-50 rounded-lg p-4">
            <h4 className="font-bold text-slate-700 mb-2">Example</h4>
            <pre className="text-sm text-slate-600 whitespace-pre-wrap font-mono">
              {architecture[selectedComponent].example}
            </pre>
          </div>
        </div>
      )}

      {/* Benefits Grid */}
      <div className="grid md:grid-cols-2 gap-6 mb-8">
        {benefits.map((benefit, idx) => (
          <div key={idx} className="bg-white rounded-xl shadow-lg p-6">
            <div className="flex items-center gap-3 mb-4">
              <benefit.icon className="w-8 h-8 text-blue-600" />
              <h3 className="text-xl font-bold text-slate-800">{benefit.title}</h3>
            </div>
            <p className="text-slate-600 mb-4">{benefit.description}</p>
            <ul className="space-y-2">
              {benefit.examples.map((ex, i) => (
                <li key={i} className="flex items-start gap-2 text-sm">
                  <span className="text-blue-500 mt-1">→</span>
                  <span className="text-slate-700">{ex}</span>
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>

      {/* Comparison */}
      <div className="bg-gradient-to-r from-green-50 to-blue-50 rounded-xl p-8">
        <h2 className="text-2xl font-bold text-slate-800 mb-6 text-center">
          Why This Beats Other Architectures
        </h2>

        <div className="space-y-4">
          <div className="bg-white rounded-lg p-4">
            <div className="flex items-center gap-3 mb-2">
              <span className="text-2xl">❌</span>
              <h4 className="font-bold text-red-700">Monolithic (Everything in ChatGPT)</h4>
            </div>
            <p className="text-slate-600 text-sm ml-11">
              ChatGPT does monitoring + queries = Impossible (can't run 24/7)
            </p>
          </div>

          <div className="bg-white rounded-lg p-4">
            <div className="flex items-center gap-3 mb-2">
              <span className="text-2xl">⚠️</span>
              <h4 className="font-bold text-yellow-700">Tight Coupling (Monitor = API)</h4>
            </div>
            <p className="text-slate-600 text-sm ml-11">
              Monitor and API in same process = One crashes, both die. Hard to scale.
            </p>
          </div>

          <div className="bg-white rounded-lg p-4">
            <div className="flex items-center gap-3 mb-2">
              <span className="text-2xl">✅</span>
              <h4 className="font-bold text-green-700">Your Architecture (Separated)</h4>
            </div>
            <p className="text-slate-600 text-sm ml-11">
              ChatGPT → Serverless API → Monitor + DB = Each component independent, scalable, reliable
            </p>
          </div>
        </div>
      </div>

      {/* Tech Stack Recommendation */}
      <div className="mt-8 bg-white rounded-xl shadow-xl p-8">
        <h2 className="text-2xl font-bold text-slate-800 mb-6">
          🛠️ Recommended Tech Stack
        </h2>

        <div className="grid md:grid-cols-4 gap-4">
          <div className="text-center p-4 bg-green-50 rounded-lg">
            <MessageCircle className="w-12 h-12 mx-auto mb-2 text-green-600" />
            <div className="font-bold text-green-700">ChatGPT</div>
            <div className="text-xs text-slate-600 mt-2">
              Built-in GPT Store
              <br />$0 cost
              <br />100M+ users
            </div>
          </div>

          <div className="text-center p-4 bg-blue-50 rounded-lg">
            <Cloud className="w-12 h-12 mx-auto mb-2 text-blue-600" />
            <div className="font-bold text-blue-700">API</div>
            <div className="text-xs text-slate-600 mt-2">
              Vercel/Netlify
              <br />AWS Lambda
              <br />$0-20/month
            </div>
          </div>

          <div className="text-center p-4 bg-purple-50 rounded-lg">
            <Eye className="w-12 h-12 mx-auto mb-2 text-purple-600" />
            <div className="font-bold text-purple-700">Monitor</div>
            <div className="text-xs text-slate-600 mt-2">
              Railway/Render
              <br />DigitalOcean
              <br />$5-15/month
            </div>
          </div>

          <div className="text-center p-4 bg-orange-50 rounded-lg">
            <Database className="w-12 h-12 mx-auto mb-2 text-orange-600" />
            <div className="font-bold text-orange-700">Database</div>
            <div className="text-xs text-slate-600 mt-2">
              Supabase
              <br />Neon.tech
              <br />$10-25/month
            </div>
          </div>
        </div>

        <div className="mt-6 p-4 bg-blue-50 rounded-lg">
          <div className="font-bold text-blue-800 mb-2">💰 Total Monthly Cost</div>
          <div className="text-sm text-slate-700">
            <strong>Startup:</strong> $15-30/month (handles 10-100 users)
            <br />
            <strong>Growth:</strong> $50-100/month (handles 1,000+ users)
            <br />
            <strong>Scale:</strong> $200-500/month (handles 10,000+ users)
          </div>
        </div>
      </div>
    </div>
  );
};

export default IdealArchitecture;